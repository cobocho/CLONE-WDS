import { style } from '@vanilla-extract/css'

export const paletteContainer = style({
  width: '1280px',
  marginInline: 'auto',
})

export const paletteColumn = style({
  width: '50%',
  maxWidth: '640px',
})
